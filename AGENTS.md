# CBWL_Adamin 仓库规则

本文件只保留全仓库必须常驻的硬规则。项目结构、开发命令、质量门禁、Git/PR 细则见 [`docs/PROJECT_ENGINEERING_GUIDE.md`](docs/PROJECT_ENGINEERING_GUIDE.md)。修改 `backend/` 或 `frontend/web/` 时，还必须阅读目标目录中更具体的 `AGENTS.md`。

## 1. 范围与事实依据

- 优先级：用户当前明确要求 > 离目标文件最近的 `AGENTS.md` > 本文件。
- 只修改完成当前任务必需的文件；不得顺手升级依赖、全仓格式化、重构无关模块或清理用户改动。
- 修改前运行 `git status --short`，识别并保留已有改动；不得覆盖、回滚或暂存不属于当前任务的内容。
- 源码、配置、测试和实际运行结果是项目事实依据；文档或图谱与其冲突时，先报告冲突并以当前可验证事实为准。

## 2. 项目边界

- 本仓库是 FastApiAdmin 衍生的前后端单仓库脚手架，不是 Java/RuoYi 项目。
- `backend/`：Python 3.12+、FastAPI、Pydantic v2、异步 SQLAlchemy、Alembic、uv、Ruff、pytest。
- `frontend/web/`：Vue 3、TypeScript strict、Vite、Element Plus、Pinia、Axios、Vitest、pnpm。
- `docker/`：MySQL、Redis、后端和 Nginx 的 Compose 编排。
- `frontend/app/` 与 `frontend/docs/` 当前是占位目录，未经明确授权不得扩展。

## 3. 低负担沟通与防幻觉

- 用户可以只用自然语言说明目标；仓库能够回答的问题应由 Agent 自行检索，不要求用户填写完整需求模板。
- 重要内容必须区分：已核实事实、用户已确认决策、基于证据的推测、暂时无法核实。不得把惯例、建议、模型记忆或 README 描述伪装成项目事实。
- 结论应能追溯到源码/配置路径、测试或运行输出、Git 记录、用户确认或官方文档；没有证据时明确说明不确定性，不编造文件、接口、字段、版本、业务规则或执行结果。
- 涉及可能变化的依赖、API、标准、安全公告或外部平台行为时，查阅当前官方来源；不得只凭训练记忆断言。
- 发现用户前提或项目资料冲突时直接指出；“证据不足”不等于“结论错误”，“能够运行”也不等于“满足需求”。
- 只有缺失信息会实质改变架构、数据语义、兼容性、安全、成本或交付结果时才询问用户；否则依据现有模式作最小、可逆假设并在交付时披露。
- 不声称未执行的测试、构建、迁移或人工验证已经通过。

## 4. 功能开发前的代码定位

1. 已知文件、类、组件或接口时，直接读取实际源码。
2. 不知道位置时，优先用 `rg` 搜索业务术语、接口 URL、路由、组件名、类型名和模型名。
3. 跨前后端、多模块、入口不明确或需要分析影响范围时，可用 `.ua/knowledge-graph.json` 和 `understand-chat`（环境可用时）辅助定位。
4. 图谱只用于发现候选入口和上下游，不是事实来源。修改前必须核实源码；不得仅凭文件名、摘要或 `imports` 推断运行时调用、字段血缘或业务数据流。
5. 图谱不可用或可能过期时，回退到源码搜索、配置、测试和运行验证，不得阻塞开发；无法确认的关系标记为“暂未核实”。
6. 跨模块或中高风险修改前，确认入口、关键调用链、跨端契约、数据/状态来源、预计修改文件和验证方式。
7. 新增、删除、移动文件或改变模块依赖后更新图谱；文案、局部样式或函数内部实现不强制更新。生成的 `.ua/knowledge-graph.json` 不得手工编辑。

## 5. 修改硬规则

- 文本文件使用 UTF-8 无 BOM、LF 和文件末尾换行；遵循目标目录现有格式与 `.editorconfig`。
- 不修改 `.git/`、依赖目录、虚拟环境、日志、缓存、构建/覆盖率输出和自动生成文件。
- 禁止提交密钥、令牌、密码、私钥、个人数据或生产连接串；日志和错误响应不得泄露敏感信息、SQL、堆栈或内部配置。
- 外部输入必须在系统边界验证；鉴权、授权和数据权限在服务端执行；数据库查询参数化，上传校验类型、大小、名称和路径。
- 不默认改变 API URL、方法、字段、响应、状态码、权限标识、数据库语义或公共类型；破坏性变更必须明确并提供迁移方案。
- 数据库变化必须评估迁移、回滚、历史数据、索引、约束和兼容性，并按需提供 Alembic 迁移。
- 沿用现有分层和相邻实现；边界层只做编排，业务和持久化逻辑进入已有对应层。禁止吞异常、假成功、请求级全局可变状态和无界并发。
- 未经明确批准，不新增生产依赖、外部服务、遥测或部署端口。
- 修复缺陷优先增加回归测试；新增行为覆盖正常路径和关键失败路径。

## 6. 验证、Git 与交付

- 先执行与改动直接相关、不会改写源码的最小检查，再按风险扩大；具体命令使用工程指南和下级 `AGENTS.md`。
- 检查失败时区分本次引入、仓库既有问题和环境缺失；不得通过删除测试、降低规则或扩大忽略项让检查变绿。
- 完成前复核 `git diff`、编码、跨端契约、迁移/配置同步以及是否混入用户原有改动。
- 未经用户明确要求，不执行 `git add`、commit、push、merge、rebase、打标签或创建 PR；禁止 `git reset --hard`、`git clean -fd` 等丢失数据的操作。
- 交付说明必须列出实际修改、实际执行的验证、未验证项和剩余风险。
